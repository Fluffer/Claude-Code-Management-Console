<ContentDialog x:Class="DevProjects.App.Views.McpViewerDialog"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:models="using:DevProjects.Core.Models"
               Title="MCP servers"
               CloseButtonText="Close"
               DefaultButton="Close">
    <ListView x:Name="ServerList"
              AutomationProperties.AutomationId="McpServerList"
              SelectionMode="None"
              IsItemClickEnabled="False"
              MinWidth="420"
              MaxHeight="360">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:McpServerInfo">
                <Grid ColumnSpacing="8" Padding="0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{x:Bind Name}"
                               Style="{StaticResource BodyStrongTextBlockStyle}"
                               TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center"/>
                    <Border Grid.Column="1" CornerRadius="9" Padding="8,2"
                            VerticalAlignment="Center"
                            Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                        <TextBlock Text="{x:Bind Transport}"
                                   FontSize="11"
                                   Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentDialog>
