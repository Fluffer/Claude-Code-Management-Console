<Window x:Class="DevProjects.App.Views.RenameProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename Project" Height="250" Width="470" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
  <StackPanel Margin="16">
    <TextBlock x:Name="CurrentText" TextWrapping="Wrap" Margin="0,0,0,10"/>
    <TextBlock Text="New name"/>
    <TextBox x:Name="NameBox" Margin="0,6,0,2" Padding="6,5"
             TextChanged="NameBox_TextChanged"
             ToolTip="The new folder name. Letters, numbers, spaces and dashes are all fine."/>
    <TextBlock x:Name="ValidationText" Foreground="#C0392B" FontSize="11" Margin="0,2,0,8"
               TextWrapping="Wrap" Text=" "/>
    <TextBlock TextWrapping="Wrap" Opacity="0.65" FontSize="11" Margin="0,0,0,12"
               Text="Note: Claude session history is tied to the folder path, so Continue will start fresh after renaming (old transcripts are not deleted). Close any session running in this folder first."/>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="OkButton" Content="Rename" Width="92" Margin="0,0,8,0" IsDefault="True"
              Click="OkButton_Click"/>
      <Button Content="Cancel" Width="92" IsCancel="True"/>
    </StackPanel>
  </StackPanel>
</Window>
