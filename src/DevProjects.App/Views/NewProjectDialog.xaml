<Window x:Class="DevProjects.App.Views.NewProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New Project" Height="280" Width="460" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
  <StackPanel Margin="16">
    <TextBlock Text="Project name"/>
    <TextBox x:Name="NameBox" Margin="0,6,0,2" Padding="6,5"
             TextChanged="NameBox_TextChanged"
             ToolTip="The folder name for your new project. Letters, numbers, spaces and dashes are all fine."/>
    <TextBlock x:Name="ValidationText" Foreground="#C0392B" FontSize="11" Margin="0,2,0,8"
               TextWrapping="Wrap" Text=" "/>
    <TextBlock Text="Create in"/>
    <ComboBox x:Name="RootCombo" Margin="0,6,0,12"
              ToolTip="Which source root the new project folder is created under"/>
    <CheckBox x:Name="LaunchCheck" Content="Start a Claude session after creating" IsChecked="True"
              Margin="0,0,0,14"
              ToolTip="Opens a Windows Terminal tab running claude in the new folder as soon as it is created"/>
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
      <Button x:Name="OkButton" Content="Create" Width="92" Margin="0,0,8,0" IsDefault="True"
              Click="OkButton_Click"/>
      <Button Content="Cancel" Width="92" IsCancel="True"/>
    </StackPanel>
  </StackPanel>
</Window>
